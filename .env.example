# Base URL of the optimizer API.
VITE_API_BASE_URL=http://localhost:4000/api/v1

# Sub-account used when the widget is opened directly rather than embedded in
# HighLevel. Inside the iframe the loader supplies the id instead.
VITE_DEFAULT_LOCATION_ID=
